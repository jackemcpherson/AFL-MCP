name: Update AFL Data

on:
  schedule:
    - cron: "0 */6 * * *"
  workflow_dispatch:
    inputs:
      season:
        description: "Season year to extract (blank = current year)"
        required: false
        type: string

concurrency:
  group: update-data
  cancel-in-progress: false

permissions:
  contents: read

jobs:
  update:
    name: Extract and load data
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - name: Checkout repository
        uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@8d2750c68a42422c14e847fe6c8ac0403b4cbd6f # v3

      - name: Build ETL image
        uses: docker/build-push-action@d08e5c354a6adb9ed34480a06d141179aa583294 # v7.0.0
        with:
          context: .
          file: etl/Dockerfile
          tags: afl-mcp-etl:latest
          load: true
          cache-from: type=gha
          cache-to: type=gha,mode=max

      - name: Run ETL
        env:
          DATABASE_URL: ${{ secrets.DATABASE_URL }}
        run: |
          SEASON="${{ inputs.season }}"
          if [ -z "$SEASON" ]; then
            SEASON=$(date +%Y)
          fi
          docker run --rm \
            --env DATABASE_URL \
            afl-mcp-etl:latest \
            sh -c "Rscript etl/extract.R $SEASON && afl-mcp db migrate && afl-mcp db load --data-dir data/raw"

      - name: Calculate PAV for current season
        env:
          DATABASE_URL: ${{ secrets.DATABASE_URL }}
        run: |
          SEASON="${{ inputs.season }}"
          if [ -z "$SEASON" ]; then
            SEASON=$(date +%Y)
          fi
          docker run --rm \
            --env DATABASE_URL \
            afl-mcp-etl:latest \
            afl-mcp db pav --year "$SEASON"

  embed:
    name: Generate incremental embeddings
    needs: update
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - name: Checkout repository
        uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@8d2750c68a42422c14e847fe6c8ac0403b4cbd6f # v3

      - name: Build embedding image
        uses: docker/build-push-action@d08e5c354a6adb9ed34480a06d141179aa583294 # v7.0.0
        with:
          context: .
          file: etl/Dockerfile.embed
          tags: afl-mcp-embed:latest
          load: true
          cache-from: type=gha,scope=embed
          cache-to: type=gha,mode=max,scope=embed

      - name: Run incremental embedding
        env:
          DATABASE_URL: ${{ secrets.DATABASE_URL }}
        run: |
          docker run --rm \
            --env DATABASE_URL \
            afl-mcp-embed:latest
